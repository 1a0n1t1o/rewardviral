import type { NextConfig } from "next";
const nextConfig: NextConfig = {
  reactStrictMode: true,
  // transpilePackages: ["@whop/react"], // keep ONLY if we actually use @whop/react
};
export default nextConfig;